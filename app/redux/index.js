// We will import all reducers and combine them here and
// default export the combined reducer in order to create the store

const initialState = {}

function iniitialReducer (state = initialState, action){
  switch(action.type){
    default:
      return state
  }
}

export default iniitialReducer
